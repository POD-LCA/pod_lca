open and close gui (close should ask to save file)

create each type of item

import database

set impacts to each type of element (and assert the impacts at various points of qty)
[Except for parameter]

Transportation: add product: change qty of both the product and the Transportation and assert

Edit name, units and life cycle of randomly selected items.

Change slider properties of a randomly selected item.

Delete items

Relationship (set a Relationship involving multiple items... move sliders and assert)

Parameter : Attache parameter to item. Change parameter values and assert

Create a second model (do a selection of above)

Save and load
