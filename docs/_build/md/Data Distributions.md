# Data Distributions

`DataDistribution` objects holds underlying `data` or `distribution` corresponding to an `attribute` of another `object` (e.g., [`Master`](products-and-processes.md#pod_lca.materials_screening.Master))

A selected set of distributions of known distribution parameters are available for use: `Uniform`, `Normal`, `Log Normal`, and `Exponential Decay`.

---
